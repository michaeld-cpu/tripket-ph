"use client";
import ComingSoon from "@/components/ComingSoon";

/**
 * Reports — placeholder while the reporting module is rebuilt.
 *
 * The previous implementation (revenue/booking charts, per-route tables and
 * the synthetic data behind them) lives in git history at 9177157 and can be
 * restored wholesale when the real reporting requirements are settled.
 */
export default function ReportsPage() {
  return <ComingSoon />;
}
